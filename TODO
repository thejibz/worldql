Handle arrays for stitching (links, fields, params.from.parent/variables)
    => params.parent done

Use params.from.variables for CSE credentials in links







------------------------------------------------------------------------------------
Switch swagger2graphql for OASgraph
    => bad 
        twitter and weather have no response type, just "string"
        twitter oaut v1a  not handled